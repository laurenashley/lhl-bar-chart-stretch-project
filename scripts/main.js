$(document).ready(function() {
  const drawBarChart = function (data, options, element) {
    let result = 'I am empty';

    return result;
  }

  let data = [],
      options = {},
      element = '';

  drawBarChart(data, options, element);

});
